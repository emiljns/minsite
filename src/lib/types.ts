export interface Metadata {
  name: string;
  slug: string;
  date: Date;
  excerpt: string;
  keywords: string[];
}

export interface PageProps {
  params: {
    entry: string;
  };
}

export interface Metadata {
  name: string;
  slug: string;
  date: Date;
  excerpt: string;
  keywords: string[];
}
